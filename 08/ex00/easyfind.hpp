#pragma once
#ifndef EASYFIND_HPP
# define EASYFIND_HPP

#include <algorithm>

template <typename T> easyfind(T where, int what)
{
	T<int>::iterator it = where.begin();
	while () //!= than where.end
	//if *it == what
	return () //return *it
}

#endif
